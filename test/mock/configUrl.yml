ulan:
  machines:
    M-tmock1:
      push:
        - url: "http://localhost/v1/push/test/{tmid}?from={from}&till={till}"
    M-tmock:
      push:
      - url: "http://localhost/v1/push/test/{tmid}?from={from}&till={till}"
      - url: "http://localhost/v1/push/test1/{tmid}?from={from}&till={till}"
